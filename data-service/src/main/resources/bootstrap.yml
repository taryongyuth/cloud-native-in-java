spring:
  application:
    name: data-service
  cloud:
    config:
      uri: http://localhost:8585

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/

ribbon:
  eureka:
    enabled: true